server:
  port: 8761

eureka:
  instance:
    hostname: ${micro.services.nutshell.eureka.uri:localhost}
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/