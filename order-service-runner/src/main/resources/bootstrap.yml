server:
  port: ${PORT:8080}

spring:
  cloud:
    config:
      enabled: true
      uri: ${CONFIGURATION_URI:http://oms-configuration.herokuapp.com}
      fail-fast: true
      retry:
        initialInterval: 2000
        multiplier: 1.2
        maxInterval: 5000
        maxAttempts: 10
