server:
  port: ${PORT:8888}
spring:
  cloud:
    config:
      discovery:
        enabled: false
      server:
        bootstrap: true
  profiles:
    include: native
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true